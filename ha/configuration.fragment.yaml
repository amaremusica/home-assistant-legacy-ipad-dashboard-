# ============================================================
# Wklej do configuration.yaml (Home Assistant)
# Potem: Ustawienia → System → Restart
# ============================================================

shell_command:
  update_ipad_panel: bash /config/www/dashboard/scripts/ha-update-panel.sh

# Opcjonalnie — dodatkowy backup co 6 h (iPad i tak woła co 30 min):
#
# automation:
#   - alias: Aktualizacja panelu iPad z Git
#     trigger:
#       - platform: time_pattern
#         hours: "/6"
#     action:
#       - service: shell_command.update_ipad_panel
